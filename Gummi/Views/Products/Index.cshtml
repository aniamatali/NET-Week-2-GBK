@model IEnumerable<Gummi.Models.Product>

@{
    ViewData["Title"] = "Product";
}

<div class="contentbackground">
<h3>Products</h3>

<table class="table table-sm table-hover table-dark">
    <tr>
        <th>
            Merchandise
        </th>
        <th></th>
    </tr>

    @foreach (var listProduct in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelProduct => listProduct.Name)
            </td>
            <td class="cellright">
                @Html.ActionLink("Edit", "Edit", new { id = listProduct.ProductId }) |
                @Html.ActionLink("Details", "Details", new { id = listProduct.ProductId }) |
                <span class="deletelink"><a>
                @Html.ActionLink("Delete", "Delete", new { id = listProduct.ProductId })
                </a></span>
            </td>
        </tr>
    }
</table>
    <h3>Top Three Reviewed Products:</h3>
    @foreach (var listproduct in Model)
    {

            <h4>@Html.ActionLink((string)listproduct.Name , "Details", new { id = listproduct.ProductId })</h4>
        }
    @Html.ActionLink("New Product", "Create")

</div>
